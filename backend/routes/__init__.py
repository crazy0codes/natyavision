# This file is intentionally left blank.
# It tells Python that the 'routes' directory should be treated as a package,
# allowing you to import modules from it (e.g., from routes.auth_routes import router).
